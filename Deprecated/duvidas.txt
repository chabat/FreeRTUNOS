#Dúvidas

Porque em um while(1) o arquivo não é escrito, mas quando a quantidade de iterações no laço é fixa (por exemplo 10), ao terminar ele escreve?

Existe um modo mais eficiente de monitorar a entrada serial?

Existe a possibilidade de perder dados ao ler dessa maneira?

Oque é um MPU Wrapper?

Arduino UNO é 8 bits? como endereça 32kb de memória então?

"Most 8 bit CPUs have 16 bit address buses allowing them to address 64kbytes,
precisely because 256 bytes really isn't enough to do very much! It just means
they need to load two bytes instead of one, each time they need to load an address.
Slightly slower but tolerable considering their size."

Se a pilha cresce para baixo, então aloca a pilha depois o tcb. Caso contrário aloca o tcb depois a pilha. Porque?

Como funciona, o que é e para que servem o trace/ing.

O que é a newlib? o que ela faz?

O arduino uno tem um microcontrolador de 8 bits, então como ele consegue endereçar 16 bits? como ele faz isso?

No FreeRTOS na pasta utility tem alguns arquivos que na pasta que surgiu quando o FreeRTOS foi instalado, não tem.

O que é o stdint.h? Ele preve o necessário para o freeRTOS ser construido, mas precisaria estar na pasta e ser renomeado para stdint.h.

O que é o pxCurrentTimerList e o pxOverflowTimerList?

O que é uma Queue storage área, e porque ele ainda cria uma queue se o tamanho de cada item for 0?
